import React from 'react';

import PageContainer from '../../../components/pageContainer';

const Campaigns: React.FC = () => {
  return (
    <PageContainer header={{ title: 'Campaigns' }}>
      <div>Campaigns</div>
    </PageContainer>
  );
};

export default Campaigns;
